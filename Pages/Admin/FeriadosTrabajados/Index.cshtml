@page
@model sueldo_rrhh.Pages.Admin.FeriadosTrabajados.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FeriadoTrabajado[0].Contrato)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FeriadoTrabajado[0].Feriado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FeriadoTrabajado[0].HorasTrabajadas)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.FeriadoTrabajado) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Contrato.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Feriado.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HorasTrabajadas)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
